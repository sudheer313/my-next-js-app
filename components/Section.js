import React from "react";
import styles from "@/styles/Home.module.css";
const Section = () => {
  return (
    <section className={styles.section}>
      <h1>Hi there this is my first next.js app</h1>
    </section>
  );
};

export default Section;
